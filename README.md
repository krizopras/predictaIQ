# PredictaIQ

Flask tabanli futbol tahmin web uygulamasi. Render Web Service icin hazirlanmistir.

## Local
`pip install -r requirements.txt`
`gunicorn backend.server:app`

## Render
`render.yaml` otomatik deployment ayarlarini icerir.
